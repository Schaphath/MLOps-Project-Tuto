
version: '3.8'

services:
  # base de données 
  postgres:
    image: postgres:15-alpine
    container_name: oncoscan_db
    restart: always
    environment:
      POSTGRES_DB: oncoscan
      POSTGRES_USER: oncoscan
      POSTGRES_PASSWORD: oncoscan_secure_password
    ports:
      - "5432:5432"
    volumes:
      # Initialisation automatique du schéma de production au premier démarrage
      - ./Database/init_modif.sql:/docker-entrypoint-initdb.d/init.sql
      # Persistance locale des données 
      - postgres_data:/var/lib/postgresql/data
    networks:
      - oncoscan_net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U oncoscan -d oncoscan"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  # Api
  api:
    build:
      context: .
      dockerfile: ./Api/Dockerfile
    image: oncoscan-api:v2.0
    container_name: oncoscan_api
    restart: always
    ports:
      - "8000:8000"
    environment:
      - DB_HOST=postgres
      - DB_NAME=oncoscan
      - DB_USER=oncoscan
      - DB_PASSWORD=oncoscan_secure_password
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - oncoscan_net
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health', timeout=5)"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 20s 

  # Interface web
  interface:
    build:
      context: .
      dockerfile: ./Interface/Dockerfile
    image: oncoscan-interface:v2.0
    container_name: oncoscan_interface
    restart: always
    ports:
      - "8501:8501"
    environment:
      - API_URL=http://api:8000/predict
    depends_on:
      api:
        condition: service_healthy
    networks:
      - oncoscan_net

# Volume
volumes:
  postgres_data:
    name: oncoscan_production_data

# Reseaux 
networks:
  oncoscan_net:
    driver: bridge

    